#
#
#

cd "$(dirname "$0")"/..

if [ -e mpid ]; then
  kill `cat mpid`
  [ $? == 0 ] && echo "operator stopped"
else
  echo "operator is already stopped"
fi

rm mpid > /dev/null 2>&1
rm pid > /dev/null 2>&1
