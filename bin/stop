#
#
#

cd "$(dirname "$0")"/..

if [ -e mpid ]; then
  kill `cat mpid`
  [ $? == 0 ] && echo "seaproxy stopped"
else
  echo "seaproxy is already stopped"
fi

rm mpid > /dev/null 2>&1
rm pid > /dev/null 2>&1
