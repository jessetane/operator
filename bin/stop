#
#
#

cd "$(dirname "$0")"/..
[ -z "$NAME" ] && NAME="seaproxy"

if [ -e mpid ]; then
  echo "stopping $NAME"
  kill `cat mpid`
else
  echo "$NAME is already stopped"
fi

rm mpid > /dev/null 2>&1
rm pid > /dev/null 2>&1
